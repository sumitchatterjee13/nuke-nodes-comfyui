# Anchors pytest's rootdir at tests/ so the repo-root __init__.py (a ComfyUI
# package with relative imports) is never collected or imported as a package.
# Run from the repo root:  python -m pytest tests
[pytest]
python_files = test_*.py
addopts = -q
